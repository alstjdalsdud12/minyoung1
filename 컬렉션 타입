#List(리스트)
#가변리스트
var numbers = [1,2,3];
numbers.add(4);
#정적 리스트
var fixedList = List<int>.filled(3,0);

#Set(중복불가)
var fruits = <String>{'사과','바나나','사과'};

#Map (키-값)
var person = {
  'name': '개발하는남자',
  'age': 37,
};
